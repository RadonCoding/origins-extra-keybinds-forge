name: Release

on:
  push:
    branches:
      - 1.19.2
  workflow_dispatch:

jobs:
  release:
    uses: RadonCoding/origins-extra-keybinds-forge/.github/workflows/release-template.yml@main
    with:
      curseforge_game_version: "Minecraft 1.19:1.19.2,Forge,environment:Client"
      modrinth_game_version: "1.19.2"
    secrets: inherit