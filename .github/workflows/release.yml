name: Release

on:
  push:
    branches:
      - 1.18.2
  workflow_dispatch:

jobs:
  release:
    uses: RadonCoding/origins-extra-keybinds-forge/.github/workflows/release-template.yml@main
    with:
      curse_game_versions: "Minecraft 1.18:1.18.2,Forge,environment:Client"
      modrinth_game_version: "1.18.2"
    secrets: inherit